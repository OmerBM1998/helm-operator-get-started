---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: podinfo-dev
  namespace: dev
  annotations:
    fluxcd.io/automated: "true"
    filter.fluxcd.io/chart-image: glob:dev-*
spec:
  releaseName: podinfo-dev
  chart:
    git: git@github.com:OmerBM1998/helm-operator-get-started/helm-operator-get-started
    path: charts/podinfo
    ref: master
  values:
    image:
      repository: omerbm/podinfo
      tag: dev-hdtwcel9
    replicaCount: 1
    hpa:
      enabled: false
